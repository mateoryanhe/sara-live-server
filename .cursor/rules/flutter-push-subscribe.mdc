---
description: Flutter App 事件订阅 — Repository 与 ViewModel 各自订 AppBus
globs: flutter-client/**/*.dart
alwaysApply: false
---

# Flutter 事件：各自订阅（禁止中心转发 UI）

统一 [AppBus]：`StartupBusEvent`（启动）与 `WsBusEvent`（WS / 本机 wsConnected）用**类型**区分；**数据缓存层与 ViewModel 分别 `appBus.add`**，不要用 AppScope 里的 `ValueNotifier`/总线再转一圈驱动页面。

## 分工

| 订阅方 | 订什么 | 做什么 |
|--------|--------|--------|
| **Repository（cache）** | `WsBusEvent`：业务 `cmd` + `PushCmd.wsConnected` | `invalidate` / 小 patch，**不碰 UI** |
| **ViewModel** | 同一批 `WsBusEvent` | `forceRefresh`（可 `silent`）拉 HTTP → 调 UI 接口 |
| **启动模块**（如 ApiDiskCache） | `StartupBusEvent.started` | 启动清理等；`started` 有 sticky |
| **AppScope** | 连/断 WS；帧 → `WsBusEvent`；create 末尾 `StartupBusEvent.started` | 不做业务失效、不通知具体页面 |

## 生命周期

- ViewModel：构造里 `appBus.add`，`dispose` 里 `remove`（页面 `dispose` 调 VM.dispose）
- Repository：在 `AppScope.create` 时绑定，`AppScope.dispose` 时解绑
- 登录 `handleLoggedIn` 连 `/ws`；登出断开并清缓存

## 禁止

```dart
// ❌ AppScope 收推送 → vipRevision++ → Page 监听再 load
scope.vipRevision.value++;

// ❌ 只有 ViewModel 订推送、Repository 不失效（易读到脏缓存）
// ❌ UI Widget 直接 appBus.add / 直接打 HTTP
```

## 推荐

```dart
// ✅ Repository / ViewModel
void _onBus(AppBusEvent e) {
  if (e is! WsBusEvent) return;
  if (e.cmd == PushCmd.vipLevel || e.cmd == PushCmd.wsConnected) {
    invalidate(userId); // 或 forceRefresh
  }
}

// ✅ 启动
void _onBus(AppBusEvent e) {
  if (e is StartupBusEvent && e.kind == StartupKind.started) {
    // 清理 API 磁盘缓存等
  }
}
```

新业务推送（金币/钻石等）照此模式各订各的。细节见 `flutter-client/lib/sara_live_core.dart` 原则 §6。
