---
description: Go 编译输出路径约束，避免生成名为 ... 的误目录
alwaysApply: true
---

## Go 编译输出

在本仓库编译 `go-src` 时：

- **正确**：输出到 `go-build/`
  - Linux：`go build -o ../go-build/xr-game-server .`（在 `go-src` 目录）
  - Windows 本地：`go build -o ../go-build/xr-game-server.exe .`
  - 或运行 `go-src/build.bat`、`pub-tool/go-测试服/deploy.bat`
- **禁止**：`-o .../xxx` 或 `-o .../xr-game-server.exe`

在 Windows 上，`...` 会被当成**字面文件夹名**，产生 `go-src/.../` 等垃圾目录（已在 `.gitignore` 忽略）。

`./...` 仅用于 **包路径**（如 `go build ./...`、`go test ./...`），**不能**用于 `-o` 输出路径。

若发现名为 `...` 的目录，**只用** .NET 字面路径删除；**禁止** `rd /s /q` 或 `Remove-Item -Recurse`（见 `workspace-safety.mdc`）：

```powershell
[System.IO.Directory]::Delete('D:\company-code\sara-live-server\go-src\module\logquery\...', $true)
```
