# Flutter client 脚本（仅 Windows）

| 目录 | 用途 |
|------|------|
| [`env/`](env/) | 配置 Flutter / JDK / Android SDK 环境变量 |
| [`apk/`](apk/) | 打 release / debug APK |

先改 `env/config.bat` 路径，再跑 `apk/build.bat`。
